# Copy to .dev.vars for local `pnpm dev` (synced into .env.local for NEXT_PUBLIC_*).
# Do not commit .dev.vars.

# Production Google OAuth web client ID (public). Client secret is API-only.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=50876205060-0j6l4c2b286q18e0vluvp7eadqcs15vq.apps.googleusercontent.com

# Chat upstream for same-origin /v1/chat/* proxy (server-side only).
# CHAT_API_ORIGIN=https://intel-copilot-v1-git-71611170957.europe-west1.run.app

# Optional:
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_APP_URL=https://local.exur.ai
# IRIS_API_ORIGIN=https://api.exur.ai
